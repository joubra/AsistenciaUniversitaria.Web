@model IEnumerable<AsistenciaUniversitaria.Web.Models.Grupo>

@{
    ViewData["Title"] = "Grupos";
}

<h1>Grupos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Agregar Grupo</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Carrera</th>
            <th>Nombre</th>
            <th>Turno</th>
            <th>Estado</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Carrera?.Nombre</td>
                <td>@item.Nombre</td>
                <td>@item.Turno</td>
                <td>@item.Estado</td>
                <td>
                    <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                    <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
